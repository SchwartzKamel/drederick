#!/bin/sh
# Replay stub for tests/fixtures/bin/fake-empire-cli — emits a deterministic
# Empire version banner so doctor's empire.installed check has a stable
# subject in fixture-driven tests.
case "$1" in
  --version|version|-v)
    echo "Empire v5.10.0"
    exit 0
    ;;
  *)
    echo "Empire v5.10.0"
    exit 0
    ;;
esac
